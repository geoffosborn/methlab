[project]
name = "methlab-common"
version = "0.1.0"
description = "Shared utilities for MethLab services"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.8.0",
    "pydantic-settings>=2.4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
